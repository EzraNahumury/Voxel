# Optional — enables WalletConnect (mobile QR) in the RainbowKit modal.
# Installed browser wallets (MetaMask, Rabby, Coinbase, OKX, ...) work without it.
# Get a free projectId at https://cloud.reown.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
